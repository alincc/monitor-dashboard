info:
  app:
    name: hystrix-dashboard
    description: Graphical view of Hystrix streams

server:
  port: 7979
  
eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
